<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Reels Generator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        h1 {
            color: #00435c;
            margin-bottom: 30px;
            font-size: 32px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        
        input[type="text"],
        textarea {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 15px;
            font-family: inherit;
            transition: border-color 0.3s;
        }
        
        input[type="text"]:focus,
        textarea:focus {
            outline: none;
            border-color: #00435c;
        }
        
        textarea {
            resize: vertical;
            min-height: 200px;
            line-height: 1.6;
        }
        
        .help-text {
            font-size: 13px;
            color: #666;
            margin-top: 5px;
        }
        
        button {
            background: #00435c;
            color: white;
            padding: 14px 32px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        button:hover {
            background: #003347;
        }
        
        button:disabled {
            background: #ccc;
            cursor: not-allowed;
        }
        
        .status {
            margin-top: 20px;
            padding: 15px;
            border-radius: 8px;
            display: none;
        }
        
        .status.success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        
        .status.error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        
        .status.loading {
            background: #d1ecf1;
            color: #0c5460;
            border: 1px solid #bee5eb;
        }
        
        .preview {
            margin-top: 30px;
            display: none;
        }
        
        .preview h2 {
            color: #00435c;
            margin-bottom: 15px;
        }
        
        .preview-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        
        .preview-item img {
            width: 100%;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .preview-item p {
            text-align: center;
            margin-top: 10px;
            font-weight: 600;
            color: #666;
        }
        
        .caption-box {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            display: none;
        }
        
        .caption-box h3 {
            color: #00435c;
            margin-bottom: 10px;
            font-size: 16px;
        }
        
        .caption-box pre {
            white-space: pre-wrap;
            word-wrap: break-word;
            font-family: inherit;
            line-height: 1.6;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Instagram Reels Generator</h1>
        
        <form id="reelForm">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" placeholder="e.g., Ultimate Rice Guide" required>
            </div>
            
            <div class="form-group">
                <label for="content">Content Lines</label>
                <textarea id="content" name="content" placeholder="Enter one item per line:
Rice — Always rinse before cooking
Garlic — Crush for maximum flavor
Pasta — Salt the water generously
Chicken — Let it rest after cooking" required></textarea>
                <div class="help-text">Enter one line per item. Use "—" or "-" to separate keyword from description.</div>
            </div>
            
            <button type="submit" id="submitBtn">Generate Reel</button>
        </form>
        
        <div id="status" class="status"></div>
        
        <div id="preview" class="preview">
            <h2>Generated Images</h2>
            <div class="preview-grid">
                <div class="preview-item">
                    <img id="thumbnailImg" alt="Thumbnail">
                    <p>Thumbnail</p>
                </div>
                <div class="preview-item">
                    <video id="reelVideo" controls style="width: 100%; border-radius: 8px;">
                        <source id="videoSource" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <p>Reel Video</p>
                </div>
            </div>
            
            <div class="caption-box" id="captionBox">
                <h3>📝 Caption</h3>
                <pre id="captionText"></pre>
            </div>
            
            <div class="actions" id="actions">
                <button onclick="validateReel()">✅ Validate & Schedule</button>
        const actions = document.getElementById('actions');
        const captionBox = document.getElementById('captionBox');
        const scheduleModal = document.getElementById('scheduleModal');
        
        let currentReelId = null;
        let currentCaption = null;
        
        function showStatus(message, type) {
            status.textContent = message;
            status.className = 'status ' + type;
            status.style.display = 'block';
        }
        
        function hideStatus() {
            status.style.display = 'none';
        }
        
        function validateReel() {
            scheduleModal.classList.add('active');
            
            // Set minimum date to today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('scheduleDate').min = today;
            document.getElementById('scheduleDate').value = today;
            
            // Set default time to current time + 1 hour
            const now = new Date();
            now.setHours(now.getHours() + 1);
            const timeString = now.toTimeString().slice(0, 5);
            document.getElementById('scheduleTime').value = timeString;
        }
        
        function closeScheduleModal() {
            scheduleModal.classList.remove('active');
        }
        
        async function confirmSchedule() {
            const scheduleDate = document.getElementById('scheduleDate').value;
            const scheduleTime = document.getElementById('scheduleTime').value;
            
            if (!scheduleDate || !scheduleTime) {
                showStatus('Please select both date and time', 'error');
                return;
            }
            
            try {
                showStatus('Scheduling reel...', 'loading');
                
                const response = await fetch('/reels/schedule', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        reel_id: currentReelId,
                        schedule_date: scheduleDate,
                        schedule_time: scheduleTime,
                        caption: currentCaption
                    })
                });
                
                if (!response.ok) {
                    const error = await response.json();
                    throw new Error(error.detail || 'Failed to schedule reel');
                }
                
                const result = await response.json();
                
                closeScheduleModal();
                showStatus(
                    `✅ ${result.message}\n💡 ${result.note}`,
                    'success'
                );
                
            } catch (error) {
                showStatus('❌ Error: ' + error.message, 'error');
            }
        }
        
        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const title = document.getElementById('title').value.trim();
            const content = document.getElementById('content').value.trim();
            
            if (!title || !content) {
                showStatus('Please fill in all fields', 'error');
                return;
            }
            
            // Parse content lines
            const lines = content.split('\n').filter(line => line.trim());
            
            if (lines.length === 0) {
                showStatus('Please add at least one content line', 'error');
                return;
            }
            
            // Prepare request
            const requestData = {
                title: title,
                content_lines: lines,
                brand: "the_gym_college",
                cta_type: "follow_part_2"
            };
            
            submitBtn.disabled = true;
            showStatus('Generating reel...', 'loading');
            preview.style.display = 'none';
            actions.style.display = 'none';
            captionBox.style.display = 'none';
            
            try {
                const response = await fetch('/reels/generate', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(requestData)
                });
                
                if (!response.ok) {
                    const error = await response.json();
                    throw new Error(error.detail || 'Failed to generate reel');
                }
                
                const result = await response.json();
                
                // Store current reel data
                currentReelId = result.reel_id;
                currentCaption = result.caption;
                
                // Show success message
                showStatus('✅ Reel generated successfully!', 'success');
                
                // Show preview
                const timestamp = new Date().getTime();
                document.getElementById('thumbnailImg').src = result.thumbnail_path + '?t=' + timestamp;
                
                // Update video source
                const videoElement = document.getElementById('reelVideo');
                const videoSource = document.getElementById('videoSource');
                videoSource.src = result.video_path + '?t=' + timestamp;
                videoElement.load(); // Reload the video element
                
                // Show caption
                document.getElementById('captionText').textContent = result.caption;
                captionBox.style.display = 'block';
                
                preview.style.display = 'block';
                actionsle.display = 'none';
            
            try {
                const response = await fetch('/reels/generate', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(requestData)
                });
                
                if (!response.ok) {
                    const error = await response.json();
                    throw new Error(error.detail || 'Failed to generate reel');
                }
                
                const result = await response.json();
                
                // Show success message
                showStatus('✅ Reel generated successfully!', 'success');
                
                // Show preview
                const timestamp = new Date().getTime();
                document.getElementById('thumbnailImg').src = result.thumbnail_path + '?t=' + timestamp;
                
                // Update video source
                const videoElement = document.getElementById('reelVideo');
                const videoSource = document.getElementById('videoSource');
                videoSource.src = result.video_path + '?t=' + timestamp;
                videoElement.load(); // Reload the video element
                
                preview.style.display = 'block';
                
            } catch (error) {
                showStatus('❌ Error: ' + error.message, 'error');
            } finally {
                submitBtn.disabled = false;
            }
        });
    </script>
</body>
</html>
